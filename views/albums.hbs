<h1>Artistas</h1>
{{#each items}} 
<div>
    <h2>{{name}}</h2>
    <img src="{{images.0.url}}" alt="{{name}}">
    <a href="/tracks/{{this.id}}" class="btn btn-primary">View tracks</a>
</div>
{{/each}}