<h1>Artistas</h1>
{{#each items}} 
<div>
    <h2>{{name}}</h2>
    <img src="{{images.0.url}}" alt="{{name}}">
    <a href="/albums/{{this.id}}" class="btn btn-primary">View albums</a>
</div>
{{/each}}